services:
  postgres:
    image: postgres:18-alpine3.23
    restart: on-failure
    env_file: '.env'
    ports:
      - '5432:5432'
    volumes:
      - <app-name>_postgres:/var/lib/postgresql

volumes:
  <app-name>_postgres:
